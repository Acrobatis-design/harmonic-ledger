package ordering

import "harmonic-ledger/code/core"

func Order(domain *core.CausalDomain) {
	// Deterministic ordering over commitments
	// No plaintext access
	// No auctions
	// No fee priority
}
